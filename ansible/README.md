```bash
$ docker-compose build
$ docker-compose up
$ docker-compose exec -it xxxx sh
$ ansible-playbook ./digitalocean/play.yml --ask-vault-pass
# running this last command will prompt you to input your password. 
```